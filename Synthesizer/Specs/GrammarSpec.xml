<grammar>
  <startSymbol>
    N
  </startSymbol>
  <nonTerminals>
    N, L, T, M, F, Z
  </nonTerminals>
  <terminals>
    last, head, access, minimum, maximum, sum, count, take, drop, sort, reverse, filter, map, zipWith, scanl1, geq, leq, eq, neq, modeq, modneq, mul, div, plus, minus, pow, min, max
  </terminals>
  <types>
    int(-3), int(-2), int(-1), int(0), int(1), int(2), int(3), list([1'2'3])
  </types>
  <production>
    <rule>
      N --> head L
    </rule>
    <rule>
      L --> list(x), [1'2'3], take L M, sort L, filter L T M, reverse L, drop L M
    </rule>
    <rule>
      M --> int(x), 1
    </rule>
    <rule>
      T --> leq
    </rule>
    <rule>
      F --> mul
    </rule>
    <rule>
      Z --> plus
    </rule>
  </production>
</grammar>